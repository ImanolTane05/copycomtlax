pipeline {
    agent any

    stages {
        stage("Compilar Docker") {
            steps {
                script {
                    sh "echo 'Compilando build...'"
                }
            }
        }

    }
}